<?xml version="1.0" encoding="utf-8" ?>
<views:MvxContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:MvvmCross.Forms.Views;assembly=MvvmCross.Forms"
             mc:Ignorable="d"
             x:Class="BudgetAppCross.Views.NewBillPage">
    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal">
                <CheckBox VerticalOptions="Center"
                  HorizontalOptions="Center"
                  IsChecked="{Binding Bill.IsPaid}"/>
                <Entry Text="{Binding Bill.AmountDue}" Keyboard="Numeric"/>
                <DatePicker Date="{Binding Bill.DueDate}"/>
                <!--<Button Text="{Binding DueDate, Converter={cvt:DateOnlyConverter}}"/>-->
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Command="{Binding CancelCommand}"/>
                <Button Text="Save" Command="{Binding SaveCommand}"/>
            </StackLayout>
        </StackLayout>
        
    </ContentPage.Content>
</views:MvxContentPage>